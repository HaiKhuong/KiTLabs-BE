# ─── Core ───
python-dotenv>=1.0.0
numpy>=1.24,<2.4
tqdm
Pillow

# ─── PyTorch (Whisper / EasyOCR / OmniVoice) ───
# PyTorch 2.9+ ép dùng torchcodec — dễ lỗi trên Linux; pin <2.9 cho OmniVoice/torchaudio.
torch>=2.2,<2.9
torchvision
torchaudio>=2.2,<2.9

# ─── Whisper ───
faster-whisper==1.2.1
ctranslate2>=4.0,<5

# ─── Step2: Gemini translation ───
google-genai

# ─── Step3: TTS ───
edge-tts

# ─── Step3: OmniVoice (optional: --step3-tts-engine omnivoice) ───
transformers>=5.3.0
huggingface_hub
underthesea
vinorm
omnivoice

# ─── OCR: EasyOCR ───
easyocr
opencv-python-headless

# ─── OCR: PaddleOCR ───
# Install paddlepaddle riêng (CPU, tránh conflict CUDA với torch):
#   pip install paddlepaddle -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
# Do NOT pip install paddlepaddle-gpu cùng venv với torch-gpu.
paddleocr>=3.4.0
rapidfuzz>=3.0.0
Levenshtein
pysrt
shapely
pyclipper
scikit-image

# ─── Image: FLUX.1 Schnell (text-to-image) ───
diffusers>=0.32.0
accelerate>=1.0.0
safetensors
