# ─── Core ───
python-dotenv>=1.0.0
numpy>=1.24,<2.4
tqdm
Pillow

# ─── PyTorch (Whisper / EasyOCR / OmniVoice) ───
# PyTorch 2.9+ ép dùng torchcodec — dễ lỗi trên Linux; pin <2.9 cho OmniVoice/torchaudio.
torch>=2.2,<2.9
torchvision
torchaudio>=2.2,<2.9

# ─── Whisper ───
faster-whisper==1.2.1
ctranslate2>=4.0,<5

# ─── Gemini (Translate Step2 + Recap Call A/B) ───
google-genai
google-generativeai>=0.8.0

# ─── Step3 / Recap TTS ───
edge-tts

# ─── Step3: OmniVoice (optional: --step3-tts-engine omnivoice) ───
transformers>=5.3.0
huggingface_hub
underthesea
vinorm
omnivoice

# ─── Step3: VoxCPM2 (optional: --step3-tts-engine voxcpm2) ───
# PyTorch ≥2.5 khuyến nghị theo upstream; pin torch ở trên vẫn dùng được nếu CUDA ok.
voxcpm>=2.0.0
soundfile

# ─── OCR: EasyOCR ───
easyocr
opencv-python-headless

# ─── OCR: PaddleOCR ───
# Install paddlepaddle riêng (CPU, tránh conflict CUDA với torch):
#   pip install paddlepaddle -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
# Do NOT pip install paddlepaddle-gpu cùng venv với torch-gpu.
paddleocr>=3.4.0
rapidfuzz>=3.0.0
Levenshtein
pysrt
shapely
pyclipper
scikit-image

# ─── Recap: shot detect + semantic cluster ───
# open_clip import name; needs torch + Pillow (above)
open-clip-torch
# TransNet V2 (soCzech) needs TensorFlow. Prefer CPU wheel if sharing GPU with torch:
#   pip install tensorflow-cpu
# otherwise:
tensorflow>=2.12
# Not on PyPI — install from upstream (provides `from transnetv2 import TransNetV2`)
# Weights are git-lfs; pip often installs pointer stubs. Recap auto-downloads real
# weights into tools/video-pipeline/.cache/transnetv2-weights (or TRANSNET_WEIGHTS_DIR).
git+https://github.com/soCzech/TransNetV2.git#egg=transnetv2
